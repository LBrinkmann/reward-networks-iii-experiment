variables:
  APP_NAME: rn-iii-backend

include:
  - project: "mpib/chm/common/deploy-pipeline"
    ref: "1.5.0"
    file: "python-app.yml"

docker:
  extends: .docker-stage
  before_script:
    - cp $CI_PROJECT_DIR/$CHILD_PIPELINE_EXECUTION_CONTEXT $CI_PROJECT_DIR
  artifacts:
    paths:
      - $CI_PROJECT_DIR
