#

image: node:12

cache:
  paths:
    - node_modules/

deploy_production:
  only:
    refs:
      - master
  stage: deploy
  environment: Production
  script:
    - npm ci
    - npm run build
    - ls -l dist
